import AppleIcon from '../../assets/AppleIcon';

const DownloadButton = () => (
  <button className="download-button" type="button">
    <AppleIcon /> <span className="download-button__text">Download the App</span>
  </button>
);

export default DownloadButton;
